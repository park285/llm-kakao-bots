{{- if eq .Count 0 -}}
{{template "empty_message" (dict "Emoji" $.Emoji.Live "Message" "현재 방송 중인 스트림이 없습니다.")}}
{{- else -}}
{{template "counted_header" (dict "Emoji" $.Emoji.Live "Label" "현재 라이브 중" "Count" .Count)}}

{{ range $index, $stream := .Streams -}}
{{- if gt $index 0}}

{{end -}}
{{template "emoji_broadcast" $}} {{$stream.ChannelName}}
   {{template "emoji_video" $}} {{$stream.Title}}
   {{template "emoji_link" $}} {{$stream.URL}}
{{- end -}}
{{- end -}}
