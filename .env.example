# Gemini (필수)
# - 단일 키: GOOGLE_API_KEY
# - 복수 키(권장, 콤마/공백 구분): GOOGLE_API_KEYS
GOOGLE_API_KEY=
# GOOGLE_API_KEYS=

# PostgreSQL (필수)
DB_PASSWORD=change_me

# HTTP API 보안 (권장)
# - mcp-llm-server-go와 game-bot-go가 공용으로 사용합니다.
# - 운영에서는 반드시 값 설정 후, docker-compose.prod.yml의 HTTP_API_KEY_REQUIRED를 true로 유지하십시오.
HTTP_API_KEY=change_me

# Admin Dashboard (필수)
# - ADMIN_PASS_HASH: bcrypt 해시 문자열을 그대로 넣습니다.
# - SESSION_SECRET: 세션(HMAC) 서명 키입니다(운영에서는 충분히 긴 랜덤 값 권장).
ADMIN_PASS_HASH=change_me
SESSION_SECRET=change_me

# Prometheus /metrics 보호 (권장)
# - admin-dashboard의 /metrics를 보호합니다.
# - docker-compose.prod.yml가 METRICS_API_KEY로 토큰 파일을 자동 생성하고,
#   Prometheus는 secrets 볼륨을 통해 해당 파일을 읽습니다:
#   ./secrets/admin-dashboard-metrics.token
METRICS_API_KEY=change_me

# gRPC (옵션)
# - 내부 통신은 기본적으로 gRPC(plaintext)로 동작합니다.
# - 운영에서는 gRPC 포트를 외부에 노출하지 말고, 내부 네트워크에서만 사용하십시오.
# - 현재는 grpcs://(TLS)를 지원하지 않습니다.
# GRPC_HOST=0.0.0.0
# GRPC_PORT=40528
# GRPC_ENABLED=true
